# Skillify Workflow

Convert a transcript summary into a reusable Claude skill.

**Implementation:** Read and follow the workflow in:
`short-form-video-transcriber/.claude/skills/skillify/SKILL.md`

That file contains the full 6-step process: select summary → analyze content → web research → choose destination → create SKILL.md → confirm.
